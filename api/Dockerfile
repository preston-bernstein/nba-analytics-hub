# This file is generated by Nx.
# Build the docker image with `npx nx docker:build @nba-analytics-hub/api`.
# Tip: Modify "docker:build" options in project.json to change docker build args.
#
# Run the container with `nx docker:run @nba-analytics-hub/api -p 3000:3000`.
#
FROM docker.io/node:lts-alpine

ENV HOST=0.0.0.0
ENV PORT=3000


WORKDIR /app

COPY dist .

# You can remove this install step if you build with `--bundle` option.
# The bundled output will include external dependencies.

# Install runtime deps and link local workspace modules copied into dist
RUN npm --omit=dev -f install \
  && npm install --no-save ./workspace_modules/@nba-analytics-hub/data-access ./workspace_modules/@nba-analytics-hub/testing

CMD [ "node", "main.js" ]
