# games-service

Upstream contract types for the Go games service. These mirror what the service returns (Game, status, team, meta) and are currently aliases to the shared canonical shapes in `../games/types` so we can track the external boundary separately.
