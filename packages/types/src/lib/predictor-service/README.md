# predictor-service

Upstream contract types for the Python predictor service (e.g., /predict request/response). These live separately from the shared prediction types so we can keep the external API shape distinct if it ever diverges.
